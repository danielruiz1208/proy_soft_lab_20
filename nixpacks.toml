phases.setup]
nixPkgs = ["php84", "php84Packages.composer"]

[phases.install]
cmds = [
  "composer install --no-interaction --prefer-dist --optimize-autoloader"
]

[start]
cmd = "php artisan optimize:clear && php artisan migrate --force && php -S 0.0.0.0:$PORT server.php"
